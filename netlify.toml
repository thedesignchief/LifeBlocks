[build]
  publish = "."
  functions = "netlify/functions"
```

Then also go to `index.html` on GitHub, click the pencil to edit, and use **Ctrl+F** to find `/api/claude` — you'll see it twice. Replace both instances with:
```
/.netlify/functions/claude
```

So instead of:
```
fetch('/api/claude',
```

It becomes:
```
fetch('/.netlify/functions/claude',
